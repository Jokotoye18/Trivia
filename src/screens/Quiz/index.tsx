import React from 'react';
import { Text, SafeAreaView } from 'react-native';

export const Quiz = (): JSX.Element => {
  return (
    <SafeAreaView>
      <Text>Hello</Text>
    </SafeAreaView>
  );
};
